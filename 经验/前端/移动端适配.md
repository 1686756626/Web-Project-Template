# 移动端适配

> 来源：Teaching-Dashboard、Finance-Manager

## 规则

用 Tailwind 响应式断点。小屏图标化导航，弹窗全屏，内容紧凑。

## 具体做法

### 导航文字隐藏

```html
<a href="#dashboard" class="nav-link">
    <i class="fa-solid fa-chart-pie mr-1.5"></i>
    <span class="hidden sm:inline">仪表盘</span>
</a>
```

`hidden sm:inline`：小于 640px 隐藏文字，只显示图标。

### 响应式网格

```html
<div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
```

### 小屏弹窗全屏

```css
@media (max-width: 640px) {
    #preview-modal > div:last-child { inset: 0 !important; border-radius: 0 !important; }
}
```

### 内容紧凑

```css
@media (max-width: 640px) {
    #content { padding: 0 8px; }
    .md-preview table { font-size: 12px; }
}
```
